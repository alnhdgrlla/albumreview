  <div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1>Create new genre</h1>
      <%= simple_form_for @genre do |g| %>
        <%= g.input :name %>
        <%= g.button :submit, class: 'btn btn-light' %>
      <% end %>
      <div class=mt-3> <%= link_to "Back Home", root_path %> </div>
    </div>
  </div>
</div>
