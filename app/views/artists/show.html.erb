<h1>All Albums Of This Artist</h1>

<% @artist.albums.each do |a| %>
  <h3><%= a.title %></h3>
  <h4> <%= link_to "<Reviews>", album_reviews_path(a) %></h4>
  <h4> <%= link_to "<Songs>", album_songs_path(a) %></h4>

<% end %>

<%= link_to 'Post Album', new_genre_album_path(@genre_id) %> <br>
<%= link_to 'Home', root_path %>

