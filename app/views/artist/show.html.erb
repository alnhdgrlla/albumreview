<h1>All Albums Of This Artist</h1>

<% @artist.albums.each do |a| %>
  <h3><%= a.title %></h3>
  <h4> <%= link_to "<Reviews>", album_review_index_path(a) %></h4>
  <h4> <%= link_to "<Songs>", album_path(a) %></h4>
<% end %>

<%= link_to 'Home', root_path %>

